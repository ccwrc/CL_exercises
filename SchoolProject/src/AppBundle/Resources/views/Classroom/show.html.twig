{% extends '::base.html.twig' %}

{% block title %} pokaż klasę {% endblock %}

{% block body %}

    <div>
        <h1> {{ class.name }} </h1>
    </div>

    <div>
        {% for pupil in class.pupils %}
            Uczeń ID: {{ pupil.id }} Nazwisko: {{ pupil.name }} <br/>
        {% endfor %}
    </div>
    {# nigdy nie uzywac phpowego var dumpa - przez odwolania moze dojsc do zapetlenia #}
    {{ dump(class) }}

{% endblock %}
