{% extends '::base.html.twig' %}

{% block title %} edycja klasy {% endblock %}

{% block body %}

    <form action="{{ path("app_classroom_changeclassroom", {"id":classroom.id}) }}" method="POST">
        <input type="text" name="className" value="{{ classroom.name }}">
        <input type="submit" value="Kliknij żeby zatwierdzić edycję">
    </form>

{% endblock %}
